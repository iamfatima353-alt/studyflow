import 'package:flutter/material.dart';

class AppColors {
  // Main StudyFlow brand color
  static const Color primary = Color(0xFF8B82D9);

  // Soft secondary color
  static const Color secondary = Color(0xFF7BB8B3);

  // Darker primary
  static const Color primaryDark = Color(0xFF756CC4);

  // Very soft primary background
  static const Color primaryLight = Color(0xFFEEEAFB);

  // Light app background
  static const Color lightBackground = Color(0xFFF8F7FC);

  // Dark app background
  static const Color darkBackground = Color(0xFF25233A);

  // Light surface
  static const Color lightSurface = Color(0xFFFFFFFF);

  // Dark surface
  static const Color darkSurface = Color(0xFF302E46);

  // Main text
  static const Color darkText = Color(0xFF25233A);

  // Text on dark backgrounds
  static const Color lightText = Color(0xFFF8F7FC);

  // Secondary text
  static const Color secondaryText = Color(0xFF77758A);

  // Success
  static const Color success = Color(0xFF75B798);

  // Error
  static const Color error = Color(0xFFD98282);

  // Warning
  static const Color warning = Color(0xFFD6AE72);
}
